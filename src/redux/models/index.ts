export interface TransactionDetails{
    // item: [];
    id: string;
    date: string;
    time: string;
    name: string;
    description: string;
    currency: string;
    type: string;
}

export interface UserState{
    visible: boolean;

}